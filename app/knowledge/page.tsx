import { Knowledge } from "@/components/knowledge";

export const metadata = {
  title: "Knowledge base — Agent Factory",
  description:
    "Curate, approve and search the authoritative content available to AI-enabled workflows.",
};

export default function KnowledgePage() {
  return <Knowledge />;
}
